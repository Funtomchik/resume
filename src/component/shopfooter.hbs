<section class="m-5">
    <div class=" d-flex col-10">
        {{#each footer}}
        <div class="nav">
            {{#each this}}
            <div>
                <a class="nav-link {{#if @last}} disabled {{/if}}" href="{{this.link}}">{{this.text}}</a>
            </div>
            {{/each}}
        </div>
        {{/each}}
    </div>
</section>