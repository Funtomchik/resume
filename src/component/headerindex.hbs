<section class="m-5">
    <div class="card">
        <div class="row">
            {{#with header}}
            <div class="col-8">
                {{#with name}}
                <h3 class="ps-3">{{firstname}} {{lastname}}</h3>
                {{/with}}
            </div>
            <div class="col-4">
                <h3 class="ps-4">{{Logo}}</h3>
            </div>
        </div>
    </div>
    {{/with}}
    </div>
    </div>
</section>